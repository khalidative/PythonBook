from math import sqrt

# ask user for input
num = eval(input("Enter number: "))

# Compute the square root with sqrt()
# sqrt() is reponsible for square root calculation,
# allowing the programmer to focus on whats important
root = sqrt(num);

# print the result
print("Square root of", num, "=", root)










































