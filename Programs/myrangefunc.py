def My_range_func(stop, start = 0, step = 1):
    """
    This function intrun calls 
    the standard range function 
    and returns it's result as 
    a list
    """
    
    li = list(range(start, stop, step))
    return li
# This raises an error