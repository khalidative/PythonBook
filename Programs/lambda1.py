# Program that defines and and uses a lambda function that 
# returns the cube of a number

cube = lambda a: a * a * a # Defining the lambda function

num = eval(input("Enter a number: "))
print(cube(num))           # using the lambda function














    















