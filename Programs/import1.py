import math

num = eval(input("Enter a number: "))
num_root = math.sqrt(num) # Using the "sqrt()" function
                          # defined in the "math" module

print("The square root of ", num, " is ", num_root)











































        